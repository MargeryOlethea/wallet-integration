"use client";

import toast from "react-hot-toast";

function Staking() {
  return (
    <>
      ini staking
      <button onClick={() => toast.success("yey!")}>Ea</button>
    </>
  );
}

export default Staking;
